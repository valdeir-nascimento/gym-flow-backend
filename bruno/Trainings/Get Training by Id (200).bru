meta {
  name: Get Training by Id (200)
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/trainings/{{trainingId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

assert {
  res.status: eq 200
  res.body.id: eq {{trainingId}}
}
