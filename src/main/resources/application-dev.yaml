spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gymflow
    username: gymflow
    password: gymflow
    hikari:
      maximum-pool-size: 10
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    br.com.gym.flow: DEBUG
    org.springframework.modulith: INFO

app:
  jwt:
    secret: dev-secret-dev-secret-dev-secret-32b
  security:
    cors:
      allowed-origins: http://localhost:3000,http://localhost:5173
